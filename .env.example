# ═══════════════════════════════════════════════════════════════════════════════
# CREDENTIALS & SECRETS ONLY
#
# All non-sensitive settings (emails, ports, timeouts, limits, etc.) are now
# managed via SQLite and editable through the Admin Dashboard → Configuración.
# On first run, SQLite is seeded with sensible defaults automatically.
# ═══════════════════════════════════════════════════════════════════════════════

# PostgreSQL (credentials)
PG_HOST=localhost
PG_USER=
PG_PASSWORD=

# MSSQL (credentials)
MSSQL_SERVER=
MSSQL_USER=
MSSQL_PASSWORD=

# Wix API (credentials)
WIX_SITE_ID=
WIX_API_KEY=

# SMTP (credentials)
SMTP_HOST=
SMTP_USER=
SMTP_PASS=

# Odoo XML-RPC (credentials)
ODOO_URL=https://your-odoo-instance.com
ODOO_DB=your-database
ODOO_USERNAME=admin
ODOO_PASSWORD=your-api-key

# Admin Dashboard (credentials)
ADMIN_PORT=3800
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# State directory (where settings.db is stored)
STATE_DIR=./state